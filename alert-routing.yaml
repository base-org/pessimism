alertRoutes:
  low:
    slack:
      - config:
          url: "url"
          channel: "#channel"

  medium:
    slack:
      - config:
          url: "url"
          channel: "#channel"
    pagerduty:
      - config:
          integration_key: "key"

  high:
    slack:
      - config:
          url: "url"
          channel: "#channel"
    pagerduty:
      - config:
          integration_key: "key"
        config_2:
          integration_key: "key2"
